namespace Entities
{
    public class Monster : Entity
    {
        public Monster(int hp, int attackPoint, int defence, int criticalPercentage, int speed) : base(hp, attackPoint, defence, criticalPercentage, speed)
        {
            
        }
    }
}